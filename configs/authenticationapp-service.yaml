apiVersion: v1
kind: Service
metadata:
  labels:
    app: authenticationapp
  name: authenticationapp
spec:
  type: NodePort
  ports:
    - name: "8082"
      port: 8082
      targetPort: 8082
      nodePort: 32000
  selector:
    app: authenticationapp
